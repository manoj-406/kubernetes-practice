# This is a service
---
apiVersion: v1
kind: Service
metadata:
  name: spc-svc
  labels:
    app: spc
spec:
  type: LoadBalancer
  selector:
    env: Dev
    app: web
   
  ports:
    - port: 80
      targetPort: 8080